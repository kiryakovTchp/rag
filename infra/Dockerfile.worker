FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml /app/
RUN pip install --no-cache-dir uv && uv pip install --system celery redis pydantic-settings sqlalchemy
COPY workers db /app/
CMD ["python","-c","print('worker skeleton — add Celery later'); import time; time.sleep(3600)"]
